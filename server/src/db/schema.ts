// Stub da Onda 0: sem tabelas ainda.
// A tabela `links` (id / original_url / short_url unique / access_count / created_at)
// é propriedade da Onda 1 (W1-T2). Este arquivo existe para que o drizzle.config
// consiga resolvê-lo sem erro de módulo inexistente.
export {};
